import 'package:flutter/material.dart';

class AppColors {
  static Color red(double opacity) => Color.fromRGBO(255, 23, 68, opacity);

  static Color neonColor(double opacity) =>
      Color.fromRGBO(0, 229, 255, opacity);
  static Color bgDark(double opacity) => Color.fromRGBO(11, 15, 26, opacity);
  static Color cardDark(double opacity) => Color.fromRGBO(21, 26, 44, opacity);
}
